// establish a switch between knots and crosses each turn

// if 3 squares in a row match. declare winner

// if all squares filled without 3 in a row. declare draw

// once square has knot or cross, don't allow change
